<ul class="wi_default">
	{% for post in posts %}
		<li><a href="{{base_url()}}posts/show/{{post.id}}">{{helper_ellip(post.title, 20)}}</a> <span>{{post.views}} Views</span></li>
	{% endfor %}
</ul>