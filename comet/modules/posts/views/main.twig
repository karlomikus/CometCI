{% for post in posts %}
	<div class="panel">
		<h4 class="subheader">{{post.title}}</h4>
		{{post.date}} in {{get_label_name(post.label)}}
		<br /><br />
		<p>
			{{post.teaser}}
			<br />
			<a href="posts/show/{{post.id}}">read more...</a>
		</p>
	</div>
{% endfor %}
