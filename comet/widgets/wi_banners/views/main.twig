{% for banner in banners %}
	<a href="{{ urldecode(banner.url) }}" target="_blank">
		<img src="{{ base_url() }}uploads/banners/{{ banner.image }}" alt="{{ banner.name }}" style="width: {{ banner.width }}px; height: {{ banner.height }}px;" />
	</a>
{% endfor %}